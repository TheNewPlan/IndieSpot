<p> <h1>Search</h1> </p>
<%= form_tag :action => 'search' do %>
    <p>What are you looking for?<br><br>
      <%= text_field_tag 'musicSearch', @mysearch %> </p>

    <% if @results > 0 %>
        <% if @music.length >0 %>
            <p> Here are the search results:</p>
            <p>
            <table>
              <tr>
                <th>Song</th>
                <th>Album</th>
                <th>Band</th>
                <th>Genre</th>
                <th>Release</th>
              </tr>
              <% @music.each do |d| %>
                  <tr>
                    <td><%= d.song %></td>
                    <td><%= d.album %></td>
                    <td><%= d.band %></td>
                    <td><%= d.genre %></td>
                    <td><%= d.release %></td>
                  </tr>
              <% end %>
            </table>
        <% else%>
            <p>There were no data points with your search criteria. Try again!</p>
        <% end %>
    <% end %>
    <%= submit_tag 'Search' %>
<% end %>